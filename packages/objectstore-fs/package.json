{
  "name": "@zeropg/objectstore-fs",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepack": "tsc -p tsconfig.build.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@zeropg/blobstore": "^0.1.0",
    "@zeropg/lease": "^0.1.0"
  },
  "peerDependencies": {
    "@electric-sql/pglite": "*"
  },
  "description": "ZeroPG core: Postgres (PGlite) with an object-storage bucket as its durable home. Incremental WAL shipping, manifest-swap commits, durability modes, streaming snapshot restore, branching, GC.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reisepass/zeropg.git",
    "directory": "packages/objectstore-fs"
  },
  "keywords": [
    "postgres",
    "pglite",
    "serverless",
    "litestream",
    "wal",
    "scale-to-zero"
  ],
  "engines": {
    "node": ">=22"
  }
}
