# Handoff / status — 2026-06-24 (pause for ~2-3 weeks)

Everything is committed + pushed. All Cloud Run services are `minScale=0` (scale to
zero, no idle cost — there are no VMs, nothing to "shut down" beyond letting them idle).

## When picking back up — priority order (per the owner)
1. **Clean up examples** — README should show only the GOOD ones (curate the live demo list).
2. **More documentation.**
3. **Make content / do marketing BEFORE pushing more code.**
4. **Then** let background agents build the fuller-featured, super-light, Supabase-compatible
   scale-to-zero DB frontend — spec is in `docs/DIY-zeroSupabaseStudioFrontend.md`.

## What's live (all scale-to-zero, on 0rs.org via free Cloud Run domain mappings)
Real-app demos: PrivateBin (~5s cold) · NocoDB (~34s) · Rallly (~21-36s) · Documenso (~30s) ·
cocoon PDS (~5s, rarely cold-starts - public AT-Proto traffic keeps it warm) · webhookx (~4-5s).
Cal.com was DROPPED (>120s cold start). Nostr: nostream+Dragonfly + znostr-relay.

Our own minimal apps (this session):
- **airtable** (`airtable-scale-to-zero.0rs.org`) — Go, rows-as-JSONB, ~8.1s cold. KEEP.
- **zeropocket** (`pocketbase-scale-to-zero.0rs.org`) — Go PocketBase-pattern, typed collections +
  relations + retention + nice SPA UI, **~3.5s cold**. KEEP. (NOT PocketBase, NOT SQLite - it's
  our code on zeropg/Postgres. Domain still misnamed `pocketbase-*`; rename to zeropocket on pickup.)
- **httpbin** (`httpbin-zeropg` + `httpbin-ui-zeropg`) — our httpbin+requestbin w/ TTL retention.
- **supabase / zubase** (`supabase-scale-to-zero.0rs.org`, svc `supabase-zeropg`) — PostgREST + GoTrue
  + RLS all verified on the single-session wire; **pgvector removed** (was crash-looping; now Ready,
  rev 00005). Components standalone: PostgREST ~0.17s, GoTrue ~0.79s (emulated). Assembled ~5-8s.

## Cold-start model (validated): `cold ≈ zeropg GCS restore (~1-6s) + app_boot`
Static Go binaries: app_boot ~1-5ms → cold start IS the restore. Node/Next.js: heavy (14-120s).
Details: `docs/COLDSTART-MODEL.md`-worthy (notes live in `docs/TODO-coldstart-and-demo-selection.md`
and `results/coldstart-apps.jsonl`). Method: read Cloud Run BOOT LOGS (public scanners re-warm the
URL, defeating clean-idle curl).

## Git map (everything is on remote `origin`)
- `main` — docs/results + the integrated history. THIS handoff snapshot includes the uncommitted
  app work (supabase + httpbin frontends) so nothing is lost.
- Demo branches (also pushed): `supabase-scale-to-zero` (airtable + supabase early),
  `worktree-agent-ad0392836e8c56ba4` (zeropocket v2), `worktree-agent-ae98adf4ee07a6074` (httpbin),
  `worktree-agent-a0baa3f9d7784e10d` (supabase full, pgvector removed), `webhookx-on-zeropg`.
- **Pickup task: reconcile/integrate these branches into a clean `main`** (the four agents worked in
  parallel worktrees + the shared dir, so versions overlap — best merged fresh, not rushed at shutdown).

## Open TODOs / deferred (don't lose these)
- **Cloudflare phase** (next, after Google-only for now): edge frontend = Cloudflare Worker/Pages as
  gatekeeper+BFF (Turnstile + secret); OR Cloudflare Tunnel+Access. Backend stays scale-to-zero on any
  platform (Cloud Run/Fly/Render/IBM). Portable anti-crawler. Cloud Run IAM auth = Google-only bonus.
- **pglite-socket fix** (`docs/TODO-pglite-socket-prepared-statements.md`): per-connection statement
  namespacing to unblock Rust sqlx/Diesel. Scope is DEEPER than the TODO says (needs a wire-protocol
  parser - see the corrected note in POSTGRES-APP-COMPAT.md). Reconcile the two docs.
- **Cursor-while-query deadlock** (single-session PGlite): issuing a dependent query while a result
  cursor is still open deadlocks under concurrency. General zeropg trap - document in POSTGRES-APP-COMPAT.md.
- **pgx on zeropg: use `default_query_exec_mode=cache_describe`** (NOT simple_protocol - breaks JSONB),
  pool > 1. Already in POSTGRES-APP-COMPAT.md.
- **zerovalkey idea**: Dragonfly (`--dir s3://`, native restore) or Valkey + our objectstore-fs as a
  scale-to-zero Redis. Dragonfly adds ~0 to cold start (boot ~70ms). For ephemeral cache, plain
  valkey-alpine is more minimal; Dragonfly earns it only for durable Redis.
- **zeropocket rename** (dir/service/domain `pocketbase-*` → `zeropocket`).
- **Spam-gate** the open-registration demos (zeropocket, zubase) with an anon-key/invite before promoting.
